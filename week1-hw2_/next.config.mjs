/** @type {import('next').NextConfig} */
const nextConfig = {};

if (process.env.NODE_ENV === 'production') {
  // Дополнительные настройки для продакшн сборки
} else {
  // Дополнительные настройки для разработки
}

export default nextConfig;
